cd "C:\Users\Win10\Desktop\sniper_bot_bsc"

ensuite je met le bon code (le log que je veux lancer je remplace lexemple test telegram:

python test_telegram.py
python sniffer.py
python dashboard.py
python dashboard.py

pour lancer les buy :
python dashboard.py
python sniffer.py
python tracker.py

Dans ta console :

bash
Copier
Modifier
pip install matplotlib

sniper_bot_bsc/
├── dashboard.py          ← ton dashboard web
├── wallet_tracker.json   ← mémoire des positions
├── sniper_memory.json    ← mémoire des trades
├── requirements.txt      ← dépendances (flask, web3, etc.)
├── Procfile              ← script de démarrage Render
├── .env (pas envoyé, à configurer dans Render)

0x46Fc325df32169b4941781FA917dC3a076194c2a